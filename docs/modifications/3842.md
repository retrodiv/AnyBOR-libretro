# OpenBOR 3842 modification inventory

This inventory records the selected upstream source and changes introduced in this history.

## Applied changes

- Libretro platform

The initial public import already includes the portable circle rasterizer and fixed-width endian helpers. Original upstream hashes remain separate from distributed hashes in `ANYBOR-SOURCE.json`.
